const { rickAndMortyApi } = require("./rickAndMortyApi");
const fs = require("fs").promises;

describe("rickAndMortyApi", () => {
  it("takes an id and returns a promise that resolves to a character's name, status and species", () => {});
});
